{
  "build": {
    "builder": "nixpacks",
    "buildCommand": "apt-get update && apt-get install -y python3 make g++ && npm install",
    "rootDirectory": "./"
  },
  "deploy": {
    "startCommand": "next start -p $PORT -H 0.0.0.0",
    "restartPolicyType": "on_failure"
  }
}